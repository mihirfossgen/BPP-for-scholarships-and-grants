import 'package:flutter/material.dart';

class Global {
  static bool isfirstlogin =
      false; // to check if the app has been installed for the first time
  static String username = "";
  static String token = ""; // received when logged
}
